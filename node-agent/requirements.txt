# Core (always needed)
numpy>=1.24
httpx>=0.27

# Config
pyyaml>=6.0            # for YAML configs (JSON works without it)

# Mesh discovery (optional — UDP-broadcast fallback works without it)
zeroconf>=0.132        # mDNS advertisement

# --- Hardware drivers: install ONLY the ones your node actually has ---
# Acoustic capture:
#   sounddevice>=0.4
# Remote ID (ESP32 serial) and GNSS (u-blox):
#   pyserial>=3.5
#   pyubx2>=1.2
# RF (SDR) — install via your OS package manager, not pip, on a Pi:
#   SoapySDR + a device module (soapysdr-module-hackrf / -rtlsdr), or pyrtlsdr
#
# Optional acoustic ML (heuristic is the zero-dependency default):
#   scikit-learn>=1.3   OR   a small torch build
